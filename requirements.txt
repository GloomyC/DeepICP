python 3.6
tensorflow >= 2.2
CUDA 10.1

--------------

DO NOT USE CONDA as linking tensorflow for compiling PointNet++ doesn't work out of box

--------------

in case of error
"assert len(gpu_devices ) > 0"
run this in terminal
export LD_LIBRARY_PATH=/usr/local/cuda-10.2/lib64${LD_LIBRARY_PATH:+:${LD_LIBRARY_PATH}}
https://stackoverflow.com/a/64472380 for more troubleshooting

--------------

to compile PointNet++ ops:
sh tf_ops/compile_ops.sh

--------------

create ./configs/project_config.json using ./configs/project_config_template.json

--------------
to create new default model
python3 train.py -mn <model_name> -new

for for more options python3 train.py --help

create new configs in their respecive folders to use as arguments











